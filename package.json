{
  "name": "shadescanai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install --legacy-peer-deps && npm run build",
    "start": "cd web && npm run preview"
  }
}
